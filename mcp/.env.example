# Copy to mcp/.env.local (gitignored) and fill in.
# URL + anon key can be omitted if the repo root .env.local already has the
# NEXT_PUBLIC_SUPABASE_* pair — the server falls back to reading that file.

SOSPHD_SUPABASE_URL=https://jnbxkvlkqmwnqlmetknj.supabase.co
SOSPHD_SUPABASE_ANON_KEY=
SOSPHD_EMAIL=juanquirozjr@gmail.com
SOSPHD_PASSWORD=
